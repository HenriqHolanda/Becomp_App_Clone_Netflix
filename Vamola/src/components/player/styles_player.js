import React from 'react'
import { StyleSheet } from 'react-native'

const style_Player = StyleSheet.create({
    Player:{
        width: '100%',
        height: 230,
        justifyContent: 'center',
        marginTop: 270,
        backgroundColor: '#000'
    }

})

export default style_Player